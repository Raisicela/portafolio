import React from "react";

const Footer = () => {
  return (
    <footer>
      <p className="max-w-max pb-3 text-left text-sm text-slate-500 sm:pb-0 mt-10">
        Build by yours trully. Build with React.js and Tailwind CSS, deployed
        with firebase.
      </p>
    </footer>
  );
};

export default Footer;
